def average(lst):
    if not lst:
        return 0
    return sum(lst) / len(lst)


def min_val(lst):
    if not lst:
        return 0
    return min(lst)


def max_val(lst):
    if not lst:
        return 0
    return max(lst)
